# Aliases
alias gs="git status"
alias gp="git pull"
alias gaa="git add -A"
gcm () {
git commit -m $1
}
ga () {
git add $1
}
gc () {
git checkout $1
}
gb () {
git checkout -b $1
}

alias sb="source ~/.bashrc"
alias eb="vim ~/.bashrc"
